/*
* To resolve the problem of queue overflow
* @params:
*   len: queue length
*   head: the head of queue is the position where the element is deleted
*   tail: the end of queue is the position where the element is inserted
*   size(): return number that is the length of queue
*   peek(): return number that is the head element of queue
*   isEmpty(): return boolean that to check the status about queue
*   enqueue(ele): receive a parameter and insert it at the end of the queue and return the queue
*   dequeue(): delete an element from the head of queue
* */

// interface CircularProps<T> {
//     len: number;
//     head: number;
//     tail: number;
// }
//
// export default class CircularQueue implements CircularProps{
//     private head: number;
//     private tail: number;
//     private len: number;
// }

export default class A{}
